<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.sajeth</groupId>
    <artifactId>m2-java-parent</artifactId>
    <version>0.0.1</version>
    <packaging>pom</packaging>
    <name>m2-java-parent</name>
    <description>Maven parent POM hierarchy for Java 25 projects: Spring Boot, Spring Cloud, gRPC, Kafka, and related stacks.</description>
    <url>https://github.com/sajeth/m2-java-parent</url>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>sajeth</id>
            <name>Sajeth</name>
            <email>sajethperli@gmail.com</email>
            <url>https://github.com/sajeth</url>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:git://github.com/sajeth/m2-java-parent.git</connection>
        <developerConnection>scm:git:ssh://github.com/sajeth/m2-java-parent.git</developerConnection>
        <url>https://github.com/sajeth/m2-java-parent</url>
        <tag>HEAD</tag>
    </scm>

    <properties>

        <buildDirectory>${project.basedir}/target.nosync</buildDirectory>
        <!-- Reproducible builds: fixes file timestamps in JARs/POMs so byte-identical artifacts
             are produced from the same source tree — see maven-artifact-plugin:check in verify -->
        <project.build.outputTimestamp>2025-01-01T00:00:00Z</project.build.outputTimestamp>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <java.version>25</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <aspectj.version>1.9.25.1</aspectj.version>
        <!-- https://mvnrepository.com/artifact/org.junit.jupiter/junit-jupiter-api -->
        <junit.version>6.0.3</junit.version>
        <!-- TDD / Mutation Testing -->
        <pitest.version>1.23.1</pitest.version>
        <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-compiler-plugin -->
        <maven.compiler.version>3.15.0</maven.compiler.version>
        <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-resources-plugin -->
        <maven.resource.version>3.5.0</maven.resource.version>
        <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-surefire-plugin -->
        <maven.surfire.plugin>3.5.5</maven.surfire.plugin>
        <!--        <base.commons.version>[1.0.0,)</base.commons.version>-->
        <!-- https://mvnrepository.com/artifact/com.fasterxml.jackson.core/jackson-databind -->
        <!-- GHSA-72hv-8253-57qq (DoS, CVSS 7.5, fixed 3.1.3); GHSA-j3rv-43j4-c7qm + GHSA-rmj7-2vxq-3g9f (RCE via PolymorphicTypeValidator, fixed 3.1.4); CVE-2026-59889 (@JsonView bypass CVSS 6.5, fixed 3.1.5) -->
        <jackson.version>3.1.5</jackson.version>
        <!-- https://mvnrepository.com/artifact/org.projectlombok/lombok -->
        <lombok.version>1.18.46</lombok.version>
        <jacoco.version>0.8.14</jacoco.version>
        <junit.jupiter.version>5.10.2</junit.jupiter.version>
        <!-- https://mvnrepository.com/artifact/com.github.spotbugs/spotbugs-maven-plugin -->
        <spotbugs.plugin.version>4.9.3.0</spotbugs.plugin.version>
        <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-pmd-plugin -->
        <pmd.plugin.version>3.26.0</pmd.plugin.version>
        <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-checkstyle-plugin -->
        <checkstyle.plugin.version>3.6.0</checkstyle.plugin.version>
        <!-- https://mvnrepository.com/artifact/org.owasp/dependency-check-maven -->
        <owasp.dependency.check.version>9.2.0</owasp.dependency.check.version>
        <!-- https://mvnrepository.com/artifact/org.cyclonedx/cyclonedx-maven-plugin -->
        <cyclonedx.plugin.version>2.7.9</cyclonedx.plugin.version>
        <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-source-plugin -->
        <maven.source.version>3.3.1</maven.source.version>
        <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-enforcer-plugin -->
        <maven.enforcer.version>3.5.0</maven.enforcer.version>
        <maven.minimum.version>3.9.0</maven.minimum.version>
        <!-- https://mvnrepository.com/artifact/io.netty/netty-bom -->
        <!-- Override io.netty: CVE-2026-47691 (CVSS 7.5), CVE-2026-45674 (CVSS 7.5), CVE-2026-44892 (CVSS 7.5), CVE-2026-45416 (CVSS 7.5),
             all fixed in 4.2.15.Final; CVE-2026-55831 (CVSS 7.5), CVE-2026-55833 (CVSS 7.5), CVE-2026-59901 (high), CVE-2026-56745 (high),
             CVE-2026-59921 (CVSS 5.7), CVE-2026-59900, CVE-2026-59899, CVE-2026-59898, CVE-2026-56746 (CVSS 6.5), netty-codec-dns memory leak,
             all fixed in 4.2.16.Final -->
        <netty.version>4.2.16.Final</netty.version>
        <!-- https://mvnrepository.com/artifact/org.apache.commons/commons-compress -->
        <!-- CVE-2024-26308 (CVSS 5.5, DoS via crafted 7z), CVE-2024-25710 (CVSS 8.1, Bzip2 infinite loop); both fixed in 1.26.0 -->
        <commons.compress.version>1.27.1</commons.compress.version>
        <!-- https://mvnrepository.com/artifact/org.apache.httpcomponents/httpclient -->
        <!-- CVE-2020-13956 (CVSS 5.3, path traversal in URIUtils); fixed in 4.5.13 -->
        <httpclient.version>4.5.14</httpclient.version>
    </properties>

    <distributionManagement>
        <repository>
            <id>github</id>
            <url>https://maven.pkg.github.com/sajeth/m2-java-parent</url>
        </repository>
    </distributionManagement>

    <repositories>
        <repository>
            <id>github</id>
            <url>https://maven.pkg.github.com/sajeth/m2-java-parent</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <scope>compile</scope>
        </dependency>
        <!-- https://mvnrepository.com/artifact/org.apache.maven.surefire/surefire-junit-platform -->
        <dependency>
            <groupId>org.apache.maven.surefire</groupId>
            <artifactId>surefire-junit-platform</artifactId>
            <version>3.5.6</version>
        </dependency>
    </dependencies>
    <build>
        <!-- Keep build output out of iCloud/Dropbox sync: inherit target.nosync for all children.
             Explicit class dirs help IDEs that don't always derive them from <directory>. -->
        <directory>${buildDirectory}</directory>
        <outputDirectory>${buildDirectory}/classes</outputDirectory>
        <testOutputDirectory>${buildDirectory}/test-classes</testOutputDirectory>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>com.github.spotbugs</groupId>
                    <artifactId>spotbugs-maven-plugin</artifactId>
                    <version>${spotbugs.plugin.version}</version>
                    <configuration>
                        <effort>Max</effort>
                        <threshold>Low</threshold>
                        <xmlOutput>true</xmlOutput>
                        <failOnError>false</failOnError>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-pmd-plugin</artifactId>
                    <version>${pmd.plugin.version}</version>
                    <configuration>
                        <printFailingErrors>true</printFailingErrors>
                        <format>xml</format>
                        <failOnViolation>false</failOnViolation>
                        <linkXRef>false</linkXRef>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-checkstyle-plugin</artifactId>
                    <version>${checkstyle.plugin.version}</version>
                    <configuration>
                        <configLocation>google_checks.xml</configLocation>
                        <consoleOutput>true</consoleOutput>
                        <failsOnError>false</failsOnError>
                        <violationSeverity>warning</violationSeverity>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.owasp</groupId>
                    <artifactId>dependency-check-maven</artifactId>
                    <version>${owasp.dependency.check.version}</version>
                    <configuration>
                        <failBuildOnCVSS>7</failBuildOnCVSS>
                        <format>ALL</format>
                        <suppressionFile>.github/owasp-suppressions.xml</suppressionFile>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.cyclonedx</groupId>
                    <artifactId>cyclonedx-maven-plugin</artifactId>
                    <version>${cyclonedx.plugin.version}</version>
                    <configuration>
                        <projectType>library</projectType>
                        <schemaVersion>1.6</schemaVersion>
                        <outputFormat>all</outputFormat>
                        <includeBomSerialNumber>true</includeBomSerialNumber>
                        <includeTestScope>false</includeTestScope>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>${jacoco.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-enforcer-plugin</artifactId>
                    <version>${maven.enforcer.version}</version>
                </plugin>
                <!-- Reproducible builds: verifies that a rebuild produces bit-identical artifacts -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-artifact-plugin</artifactId>
                    <version>3.5.2</version>
                </plugin>
                <!-- Dependency license report: generates THIRD-PARTY.txt listing transitive dep licenses -->
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>license-maven-plugin</artifactId>
                    <version>2.5.0</version>
                    <configuration>
                        <outputDirectory>${project.build.directory}</outputDirectory>
                        <thirdPartyFilename>THIRD-PARTY.txt</thirdPartyFilename>
                        <failIfWarning>false</failIfWarning>
                        <includeTransitiveDependencies>true</includeTransitiveDependencies>
                        <excludedScopes>test,provided</excludedScopes>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <executions>
                    <execution>
                        <id>enforce-maven-version</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireMavenVersion>
                                    <version>[${maven.minimum.version},)</version>
                                </requireMavenVersion>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven.compiler.version}</version>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven.surfire.plugin}</version>
                <configuration>
                    <testFailureIgnore>true</testFailureIgnore>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-resources-plugin</artifactId>
                <version>${maven.resource.version}</version>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals><goal>prepare-agent</goal></goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>verify</phase>
                        <goals><goal>report</goal></goals>
                    </execution>
                </executions>
            </plugin>
            <!-- Reproducible builds: record build metadata so artifacts can be verified -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-artifact-plugin</artifactId>
                <executions>
                    <execution>
                        <id>buildinfo</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>buildinfo</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
    <modules>
        <module>m2-core-parent</module>
        <module>m2-springboot-parent</module>
    </modules>

</project>
